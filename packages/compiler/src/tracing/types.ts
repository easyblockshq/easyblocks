/**
 * All props that are passed to traceable component.
 */
type TraceableComponentProps = {
  traceId: string;
  traceClicks: boolean;
  traceImpressions: boolean;
};

export type { TraceableComponentProps };
